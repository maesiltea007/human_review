import 'package:flutter/material.dart';

class OtherUserPage extends StatelessWidget {
  const OtherUserPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Other User Page"),
      ),
    );
  }
}